syntax = "proto3";

package informaticsmatters.protobuf.accountserver;

// Commodity enum.
enum CommodityEnum {
    // Every enum _must_ have a zero.
    MEMORY = 0;
    STORAGE = 1;
    PROCESSING = 2;
}
